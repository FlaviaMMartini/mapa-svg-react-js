import React from 'react'
import { storiesOf } from '@storybook/react'
import { MapTooltipContactUs } from 'components'

storiesOf('MapTooltipContactUs', module)
  .add('default', () => (
    <MapTooltipContactUs />
  ))
